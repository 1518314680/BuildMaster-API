server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/buildmaster?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  redis:
    host: localhost
    port: 6379

ai:
  provider: deepseek
  api-key: ${AI_API_KEY:your-key-here}

logging:
  level:
    root: INFO
